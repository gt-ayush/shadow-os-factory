#!/bin/bash

echo "Setting Plymouth theme..."

# Ensure plymouth is installed (safe)
apt-get update
apt-get install -y plymouth plymouth-themes

# Set your theme
plymouth-set-default-theme shadow -R || true
